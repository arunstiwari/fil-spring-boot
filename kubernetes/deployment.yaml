apiVersion: apps/v1
kind: Deployment
metadata:
  name: nginx-session
spec:
  replicas: 3
  selector:
    matchLabels:
      tier: frontend
  template:
    metadata:
      labels:
        tier: frontend
    spec:
      containers:
        - name: nginx-session
          image: nginx